defaults:
  - common
  - _self_

hydra:
  mode: MULTIRUN
  sweep:
      dir: output/dsprites_simple/${now:%Y-%m-%d_%H-%M-%S}
  sweeper:
    params:
      seed: 1 #, 2, 3, 4, 5
      dataset: dsprites_simple
      model: blackbox, cbm_linear

# Bigger backbone for real world datasets
img_backbone_name: facebook/dinov2-base

gpus: [1]