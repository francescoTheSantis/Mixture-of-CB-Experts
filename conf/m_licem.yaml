defaults:
  - common
  - _self_

hydra:
  mode: MULTIRUN
  sweep:
      dir: output/m_licem/${now:%Y-%m-%d_%H-%M-%S}
  sweeper:
    params:
      seed: 1, 2, 3, 4, 5
      dataset: xor, cub, mnist_addition, cebab, awa2, cub_incomplete, awa2_incomplete
      model: m_licem, m_licem_global