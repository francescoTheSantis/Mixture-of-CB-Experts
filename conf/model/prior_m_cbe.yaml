defaults:
  - common
  - _self_
  
params:
  _target_: "src.models.prior_m_cbe.PriorMCBE"
  task_penalty: ${task_penalty}
  concept_penalty: ${concept_penalty}
  int_prob: ${int_prob}
  memory_size: ${memory_size}
  embedding_size: ${embedding_size}
  mc_approx: 1

metadata:
  name: prior_m_cbe