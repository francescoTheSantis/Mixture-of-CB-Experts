defaults:
  - common
  - _self_
  
params:
  _target_: "src.models.m_licem.LinearMemoryReasoner"
  task_penalty: ${task_penalty}
  int_prob: ${int_prob}
  memory_size: ${memory_size}
  negative_concepts: True
  embedding_size: ${embedding_size}
  embedding_memory: True
  intervene_on_selection: True
  linear_classifier_selection: False
  sampling: False
  cos_sim: False

metadata:
  name: m_licem