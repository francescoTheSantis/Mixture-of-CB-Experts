defaults:
  - common
  - _self_
  
params:
  _target_: "src.models.m_licem.LinearMemoryReasoner"
  task_penalty: ${task_penalty}
  int_prob: ${int_prob}
  memory_size: 7
  negative_concepts: False
  weight_reg: 1e-4
  embedding_size: ${embedding_size}
  sampling: True
  use_bias: True
  embedding_memory: True
  intervene_on_selection: False

metadata:
  name: m_licem_no_c_emb