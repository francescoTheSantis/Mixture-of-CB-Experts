defaults:
  - common
  - _self_
  
params:
  _target_: "src.models.sym_cmr.SymbolicMemoryReasoner"
  task_penalty: ${task_penalty}
  int_prob: ${int_prob}
  memory_size: ${memory_size}
  embedding_size: ${embedding_size}
  mc_approx: 10
  weight_reg: 1e-5
  embedding_memory: True
  equation_learning_strategy: "sym_reg_alg" 

metadata:
  name: m_sym_cmr_sr