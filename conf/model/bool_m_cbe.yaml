defaults:
  - common
  - _self_
  
params:
  _target_: "src.models.bool_m_cbe.BoolMCBE"
  task_penalty: ${task_penalty}
  concept_penalty: ${concept_penalty}
  int_prob: ${int_prob}
  memory_size: ${memory_size}
  embedding_size: ${embedding_size}
  mc_approx: 1
  # Update PySR parameters as needed
  # pysr_params:
  #   niterations: 1
  #   populations: ...
  #   population_size: ...
  #   ncycles_per_iteration: ...

metadata:
  name: bool_m_cbe
