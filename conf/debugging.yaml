defaults:
  - common
  - _self_

hydra:
  mode: MULTIRUN
  sweep:
      dir: test/${now:%Y-%m-%d_%H-%M-%S}
  sweeper:
    params:
      seed: 1 #, 2, 3
      dataset: synthetic_motion #mawps
      model: sr_sym_cbm #sr_sym_cbm, prior_sym_cbm, kan_sym_cbm
      memory_size: 4
    
gpus: [1]

scale_variables: False

max_epochs: 500
patience: 50

use_stored_dataset: True
extract_embeddings: True

wandb:
  entity: null
  project: null

note: test