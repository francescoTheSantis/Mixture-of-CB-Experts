defaults:
  - common
  - _self_

hydra:
  mode: MULTIRUN
  sweep:
      dir: test/${now:%Y-%m-%d_%H-%M-%S}
  sweeper:
    params:
      seed: 1
      dataset: dsprites_simple #, dsprites_simple, dsprites_complex, pendulum, cub, cub_incomplete, awa2, awa2_incomplete, cifar10, cifar100
      model: sym_cmr_kan
      memory_size: 1
    
gpus: [0]

max_epochs: 10
patience: 20

use_stored_dataset: True
extract_embeddings: True

disjoint_training: True

# wandb:
#   entity: null
#   project: null

note: test