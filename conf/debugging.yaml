defaults:
  - common
  - _self_

hydra:
  mode: MULTIRUN
  sweep:
      dir: test/${now:%Y-%m-%d_%H-%M-%S}
  sweeper:
    params:
      seed: 1
      dataset: cub #, awa2_incomplete, awa2, cub, cub_incomplete 
      model: sr_sym_cbm
      memory_size: 1
    
gpus: [0]

# img_backbone_name: resnet18

# NOTE: check this variable before running experiments 
disjoint_training: True
hard_concepts: True

max_epochs: 200
patience: 50

use_stored_dataset: True
extract_embeddings: True

wandb:
  entity: null
  project: null

note: test