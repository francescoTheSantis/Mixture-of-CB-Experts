defaults:
  - common
  - _self_
  
hydra:
  mode: MULTIRUN
  sweep:
      dir: output/prior_reg/${now:%Y-%m-%d_%H-%M-%S}
  sweeper:
    params:
      model: prior_sym_cbm
      dataset: dsprites_simple, pendulum, dsprites_complex, mnist_arithmetic, mawps
      seed: 1, 2, 3, 4, 5

note: "Memory-reg" 