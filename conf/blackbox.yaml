defaults:
  - common
  - _self_

hydra:
  mode: MULTIRUN
  sweep:
      dir: output/blackbox/${now:%Y-%m-%d_%H-%M-%S}
  sweeper:
    params:
      seed: 1, 2, 3, 4, 5
      #dataset: cub, cub_incomplete, awa2, awa2_incomplete, mnist_arithmetic, dsprites_simple, dsprites_complex, pendulum
      dataset: cub, cub_incomplete, awa2, awa2_incomplete
      memory_size: 0
      model: blackbox