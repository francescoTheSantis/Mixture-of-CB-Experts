defaults:
  - common
  - _self_

loader: 
  name: mnist_addition
  batch_size: 1024

# Dataset parameters
metadata:
  task: classification
  lr: 0.001
  name: mnist_addition
  data_type: image
  concept_type: binary

latent_size: 128