defaults:
  - common
  - /encoder: mlp
  - _self_

loader: 
  name: xor

# Dataset parameters
metadata:
  task: classification
  lr: 0.01
  name: xor
  data_type: toy
  concept_type: binary

latent_size: 8

hard_concepts: true